version: 1
workspace:
  notes_dir: inputs/notes
  prompts_dir: inputs/prompts/guangdong
  rubrics_dir: inputs/rubrics
  knowledge_dir: inputs/knowledge
  examples_dir: inputs/examples
sessions_dir: sessions
filenames:
  allow_unicode: true
  max_slug_len: 16
evaluation:
  language: zh-CN
  dimensions:
    - content_structure
    - logic_coherence
    - etiquette_expression
    - professionalism
    - policy_alignment
    - clarity_concision
retrieval:
  top_k: 5
  min_score: 0.15
