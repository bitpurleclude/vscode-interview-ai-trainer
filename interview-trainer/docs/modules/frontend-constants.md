# 前端常量（frontend-constants）

## 模块定位与职责
集中维护默认状态、枚举和固定配置。

## 目录与关键文件
- `webview/src/constants/defaultState.ts`：默认步骤与初始状态

## 注意事项
- 前后端步骤顺序变更需同步 `src/interviewTrainer/application/services/it_progress.ts`
