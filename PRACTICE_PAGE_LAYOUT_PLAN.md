# 练习页布局规划（Practice Page Layout Plan）

## 目标
- 状态区改为三列网格，并按指定顺序展示
- 面试评价整行展示，实时输出三列横排；无输出时不渲染三列框
- PracticeFlow 中“进度/题干/题目区”放在状态区下方
- 保持现有功能不变，仅调整布局与展示逻辑

## 布局示意（单一区域）
```
┌──────────────────────────┬──────────────────────────┬──────────────────────────┐
│ ● 初始化 100%             │ ● 录音中 100%             │ ● 声学分析 100%           │
├──────────────────────────┼──────────────────────────┼──────────────────────────┤
│ ● 题目解析 100%           │ ● 多题分析 25%            │ ● 语音转写 100%           │
├──────────────────────────────────────────────────────────────────────────┤
│ ● 面试评价（整行）                                                        │
│  ┌──────────────┬──────────────┬──────────────┐                             │
│  │ 第1题实时输出 │ 第2题实时输出 │ 第3题实时输出 │                             │
│  └──────────────┴──────────────┴──────────────┘                             │
│  （无输出时不渲染三列实时框）                                              │
├──────────────────────────┼──────────────────────────┼──────────────────────────┤
│ ● 笔记学习 50%             │ ● 结果生成               │ ● 文件写入               │
└──────────────────────────┴──────────────────────────┴──────────────────────────┘
```

## 状态顺序（step id）
1. init（初始化）
2. recording（录音中）
3. acoustic（声学分析）
4. question（题目解析）
5. segment（多题分析）
6. asr（语音转写）
7. evaluation（面试评价）
8. notes（笔记学习）
9. report（结果生成）
10. write（文件写入）

## 组件修改清单（按文件）
### 1) `interview-trainer/webview/src/components/practice/PracticeFlow.tsx`
- 取消左右分栏结构，改为单列容器
- 状态区（StepsList）放在上方
- 进度条 / 音频时长 / thinking / 题干输入 / 题目列表 / 笔记命中 统一放在状态区下方
- 保持原有交互（识别题目、笔记命中展开、等）逻辑不变

### 2) `interview-trainer/webview/src/components/practice/StepsList.tsx`
- 固定步骤顺序为“状态顺序（step id）”中的列表
- evaluation 行整行全宽（grid-column: 1 / -1）
- evaluation 实时输出框仅在以下任一条件成立时显示：
  - 任意题目有输出文本
  - evaluation 步骤状态为 running
- 其他步骤的实时输出（StreamCard）逻辑不变

### 3) `interview-trainer/webview/src/components/StreamCard.tsx`
- 若评价卡在三列中出现视觉拥挤，补充最小样式微调（留白/字号/行高）
- 无需改动逻辑，仅适配样式（视实际效果）

### 4) `interview-trainer/webview/src/styles.css`
- `.it-flow` 改为单列布局
- `.it-steps` 改为三列网格（gap、卡片高度一致）
- `.it-step--evaluation` 设置整行跨度（grid-column: 1 / -1）
- `.it-evaluation__stream-grid` 设置三列横排（等宽）
- 适配 `.it-question` / `.it-progress` / `.it-thinking` 的上下间距

## 展示规则
- 面试评价整行始终显示标题与状态
- 评价实时输出（三列卡片）仅在有输出或运行中时显示
- 笔记学习位置下移到最后一行左侧

## 风险与注意
- 步骤排序由前端强制排序，需确保 step id 在状态列表中完整覆盖
- 若有新步骤新增，应同步更新“状态顺序”列表
- 样式变更可能影响移动端展示，需要额外检查

## 验收清单
- 状态区三列布局正确，顺序与示意一致
- 面试评价整行显示，三列实时输出在有内容时出现
- 进度/题干/题目区位于状态区下方
- 其他功能不受影响（录音、识别题目、笔记命中、结果展示）
